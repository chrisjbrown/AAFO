<div class="form-group">
    <label>{{localize "AAFO.AdvancementCustomTitle"}}</label>
    <div class="form-fields">
        <input type="text" name="title" value="{{src.title}}" placeholder="{{default.title}}">
    </div>
</div>

<div class="form-group">
    <label>{{localize "AAFO.AdvancementCustomIcon"}}</label>
    <div class="form-fields">
        {{filePicker type="image" target="icon"}}
        <input type="text" name="icon" value="{{src.icon}}" placeholder="{{default.icon}}">
    </div>
</div>

{{!-- {{#if showClassRestrictions}}
<div class="form-group">
    <label>{{localize "AAFO.AdvancementClassRestriction"}}</label>
    <div class="form-fields">
        <select name="classRestriction">
            {{#select classRestriction}}
            <option value="">{{localize "AAFO.AdvancementClassRestrictionNone"}}</option>
            <option value="primary">{{localize "AAFO.AdvancementClassRestrictionPrimary"}}</option>
            <option value="secondary">{{localize "AAFO.AdvancementClassRestrictionSecondary"}}</option>
            {{/select}}
        </select>
    </div>
</div>
{{/if}} --}}

{{#if showLevelSelector}}
<div class="form-group">
    <label>{{localize "AAFO.Level"}}</label>
    <div class="form-fields">
        <select name="level" data-dtype="Number">
            {{selectOptions levels selected=level}}
        </select>
    </div>
</div>
{{/if}}
