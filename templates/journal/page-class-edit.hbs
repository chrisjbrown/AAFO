<form class="{{ cssClass }} flexcol drop-target" autocomplete="off">
    {{> journalEntryPageHeader }}

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.AAFO.Class.Item" }}</label>
        <div class="form-fields">
            <ol class="items-list">
                {{#if document.system.item}}
                <li class="item flexrow" data-item-uuid="{{ document.system.item }}" data-item-type="linked">
                    <div class="item-name">{{{ aafo-linkForUuid document.system.item }}}</div>
                    <div class="item-controls flexrow">
                        <a class="item-control item-delete" data-tooltip="AAFO.ItemDelete"
                           aria-label="{{ localize 'AAFO.ItemDelete' }}">
                            <i class="fa-solid fa-trash" inert></i>
                        </a>
                    </div>
                </li>
                {{else}}
                <li class="item flexrow">{{ localize "JOURNALENTRYPAGE.AAFO.Class.ItemHint" }}</li>
                {{/if}}
            </ol>
        </div>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.AAFO.Class.Description" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.value">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.AAFO.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.AAFO.Class.DescriptionHint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.AAFO.Class.AdditionalHitPoints" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.additionalHitPoints">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.AAFO.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.AAFO.Class.AdditionalHitPointsHint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.AAFO.Class.AdditionalTraits" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.additionalTraits">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.AAFO.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.AAFO.Class.AdditionalTraitsHint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.AAFO.Class.AdditionalEquipment" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.additionalEquipment">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.AAFO.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.AAFO.Class.AdditionalEquipmentHint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.AAFO.Class.SubclassHeader" }}</label>
        <div class="form-fields">
            <input type="text" name="system.subclassHeader" value="{{system.subclassHeader}}">
        </div>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.AAFO.Class.SubclassDescription" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.subclass">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.AAFO.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.AAFO.Class.SubclassDescriptionHint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.AAFO.Class.SubclassItems" }}</label>
        <div class="form-fields">
            <ol class="items-list">
                {{#each subclasses}}
                <li class="item flexrow" data-item-uuid="{{this.document.uuid}}" data-item-type="subclass">
                    <div class="item-name">{{{ aafo-linkForUuid this.document.uuid }}}</div>
                    <div class="item-controls flexrow">
                        <a class="item-control item-delete" data-tooltip="AAFO.ItemDelete"
                           aria-label="{{ localize 'AAFO.ItemDelete' }}">
                            <i class="fas fa-trash" inert></i>
                        </a>
                    </div>
                </li>
                {{else}}
                <li class="item flexrow">{{ localize "JOURNALENTRYPAGE.AAFO.Class.SubclassHint" }}</li>
                {{/each}}
            </ol>
        </div>
    </div>
</form>
