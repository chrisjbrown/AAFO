<div class="aafo2 chat-card request-card">
    <div class="card-buttons">
        <button {{aafo-dataset dataset}}>
            <span class="visible-dc">{{{buttonLabel}}}</span>
            <span class="hidden-dc">{{{hiddenLabel}}}</span>
        </button>
    </div>
</div>
