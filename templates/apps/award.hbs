<form autocomplete="off">
    <header>{{ localize "AAFO.Award.Title" }}</header>
    <div class="distribution">
        <span>{{localize "AAFO.Award.Distribution.Split"}}</span>
        <slide-toggle name="each" {{checked each}}></slide-toggle>
        <span>{{localize "AAFO.Award.Distribution.Each"}}</span>
    </div>
    {{#unless hideXP}}
    <label class="xp currency-style">
        <span class="roboto-upper">{{localize "AAFO.ExperiencePoints"}}</span>
        {{numberInput xp name="xp" class="uninput" min=0 step=1}}
    </label>
    {{/unless}}
    <section class="currency">
        {{#each currency}}
        <label aria-label="{{label}}">
            <i class="currency {{ @key }}" data-tooltip="{{ label }}"></i>
            {{numberInput value name=(concat "amount." @key) class="uninput" min=0 step=1}}
        </label>
        {{/each}}
    </section>
    <div class="destinations">
        {{#if noPrimaryParty}}
        <p class="no-primary-party">{{localize "AAFO.Award.NoPrimaryParty"}}</p>
        {{/if}}
        {{#each destinations}}
        <label>
            <input type="checkbox" name="destination.{{doc.id}}" {{checked checked}}>
            {{{icon}}} {{doc.name}}
        </label>
        {{else}}
        <p class="no-destinations">{{localize "AAFO.Award.NoDestinations"}}</p>
        {{/each}}
    </div>
    <button type="submit" name="transfer" disabled>
        <i class="fa-solid fa-trophy"></i> {{localize "AAFO.Award.Action"}}
    </button>
</form>
