<form autocomplete="off">
    <p class="notes">{{localize "AAFO.InitiativeConfigHint"}}</p>

    <div class="form-group">
        <label>{{localize "AAFO.AbilityModifier"}}</label>
        <div class="form-fields">
            <select name="system.attributes.init.ability">
                {{selectOptions abilities selected=ability blank="" labelAttr="label"}}
            </select>
        </div>
    </div>

    <div class="form-group">
        <label>{{localize "AAFO.InitiativeBonus"}}</label>
        <div class="form-fields">
            <input name="system.attributes.init.bonus" type="text" value="{{bonus}}">
        </div>
    </div>

    <div class="form-group">
        <label>{{localize "AAFO.FlagsAlert"}}</label>
        <input type="checkbox" name="flags.aafo.initiativeAlert" {{checked initiativeAlert}}>
    </div>

    <div class="form-group">
        <label>{{localize "AAFO.FlagsInitiativeAdv"}}</label>
        <input type="checkbox" name="flags.aafo.initiativeAdv" {{checked initiativeAdv}}>
    </div>

    <button type="submit"><i class="far fa-save"></i> {{ localize "Submit"}}</button>
</form>
